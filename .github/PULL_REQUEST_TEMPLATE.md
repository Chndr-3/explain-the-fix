## What this changes

<!-- One or two sentences. -->

## Checklist

- [ ] If `references/style-reference.html` changed, I opened it in a browser and checked both themes + a glossary term click-through
- [ ] If a plugin manifest changed (`.claude-plugin/*.json`, `.codex-plugin/plugin.json`, `.agents/plugins/marketplace.json`), `name`/`version` stay in sync across them and the JSON is valid
- [ ] If a new shape/workflow step was added, `SKILL.md`'s table and `references/renderer.md` were both updated
- [ ] Diagrams stay within the 8-node, one-diagram-per-explanation budget
